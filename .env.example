# ---------------------------------------------------------------- required

# The first account. Read only when the users table is empty, so this is a seed:
# after the first start, manage accounts on the Users page of the web UI and this
# pair is ignored. Once you have signed in you can blank OPDS_PASSWORD here.
#
# Plaintext on purpose. Password hashes ($argon2id$..., $2b$...) all begin with `$`,
# which Docker Compose interpolates inside an env_file, so a pasted hash reaches the
# container mangled and nobody can log in. Avoid `$` in this value for the same reason.
OPDS_USERNAME=reader
OPDS_PASSWORD=

# The externally visible base URL. Used to build the links inside the OPDS feed,
# so it must match what the reader connects to, with no trailing slash.
PUBLIC_URL=https://books.example.com

# ---------------------------------------------------------------- ingest

# Comma-separated bearer tokens for POST /api/articles (phone shortcuts, bookmarklets).
# Generate one with: openssl rand -base64 24
API_TOKENS=

# ---------------------------------------------------------------- server

HOST=127.0.0.1
PORT=8080
DATA_DIR=./data
CATALOG_TITLE=Articles
CATALOG_AUTHOR=opds-feed
LOG_LEVEL=info

# Honour X-Forwarded-Proto / X-Forwarded-Host from your reverse proxy.
TRUST_PROXY=true

# ---------------------------------------------------------------- catalogue

# Entries per OPDS page. Small readers cope badly with very long feeds.
FEED_PAGE_SIZE=25
# How far back the "Latest" shelf reaches.
FEED_RECENT_DAYS=14

# ---------------------------------------------------------------- conversion

# Reader-compatibility knobs. The defaults suit small greyscale e-ink screens.
# greyscale | colour
EPUB_IMAGE_MODE=greyscale
EPUB_EMBED_IMAGES=true
EPUB_MAX_IMAGE_WIDTH=800
EPUB_MAX_IMAGES=40
EPUB_JPEG_QUALITY=72
EPUB_COVER_WIDTH=600
EPUB_COVER_HEIGHT=800
EPUB_LANGUAGE=en

# ---------------------------------------------------------------- fetching

FETCH_TIMEOUT_MS=30000
# Measured: a big Wikipedia page is ~2 MB of HTML and peaks the container at ~340 MB.
# 5 MB leaves real articles room while still bounding a pathological page.
FETCH_MAX_BYTES=5242880
# Leave false unless you deliberately want to ingest from your own LAN.
# Turning this on removes the SSRF protection on the ingest API.
FETCH_ALLOW_PRIVATE_ADDRESSES=false

# ---------------------------------------------------------------- tagging

# Optional. With a key, every converted article is filed under up to three of the
# tags you list on the site's Tags page, by the model below via OpenRouter. No key,
# no tagging — nothing else changes. Any OpenRouter chat model id works, and so does
# TypeSafe's Jev decision model (typesafe/jev-1.13), which is asked one yes/no per tag
# and cannot answer with a tag it was not given.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=typesafe/jev-1.13

# ---------------------------------------------------------------- queue & feeds

# One at a time: each conversion holds a full jsdom DOM. Raise only if RAM allows.
QUEUE_CONCURRENCY=1
QUEUE_MAX_ATTEMPTS=3
QUEUE_RETRY_DELAY_MS=30000

RSS_ENABLED=true
RSS_POLL_INTERVAL_MINUTES=30
RSS_MAX_ITEMS_PER_POLL=10
